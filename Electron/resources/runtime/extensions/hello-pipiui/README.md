# hello-pipiui

Bundled dogfood example for PipiUI dual-half extensions (`id`: `hello-pipiui`). Enable it from **Settings → Extensions**; it is not a production feature, just the in-tree package the loader discovers as a built-in L1 sample (one tool, one command, a tool-panel entry, and a namespaced settings schema).
